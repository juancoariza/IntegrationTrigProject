//import java.util.InputMismatchException;

public class IdeasStorage {
  
  /* sideAngleUnite[0][0] = MissingInfo.sides[0];
  sideAngleUnite[0][1] = MissingInfo.sides[1];
  sideAngleUnite[0][2] = MissingInfo.sides[2];
  sideAngleUnite[1][0] = MissingInfo.angles[0];
  sideAngleUnite[1][1] = MissingInfo.angles[1];
  sideAngleUnite[1][2] = MissingInfo.angles[0]; */
    
//while (terminator == 1) {
  
  //System.out.println("Would you like to use the program again? Enter 1 for yes, "
     // + "or 0 for no");
  
 // String order = search.nextLine();
  
  //try {
    
    //Integer.valueOf(order);
    
  //}
  
  //catch {
    
  //}
  
  //finally {
    //System.out.println("The program continues\n");
  //}
  
 /* if (Integer.valueOf(order) == 0) {
    
    System.out.println("Thank you for using the program! Don't forget to "
        + "save your answers before closing.\n");
    break;
    
  }
  
  
} */
  
  /* try {
    
    while (angle > 180 || angle <= 0 || valueA <= 0 || valueB <= 0) {
      // The initial angle cannot be greater than 180 degrees
      //Improve this method validation. Some of the code is redundant 
      //Create an input checker class if possible to avoid this setup
      if (angle > 180 || angle <= 0) {

        System.out.println("Sorry! The angle cannot be greater"
            + " than 180 degrees. Try again. ");
        break;

      } else if (valueA <= 0 || valueB <= 0) {
        // The two sides of the triangle cannot be negative

        System.out.println(
            "Sorry! The sides cannot be negative or 0." + " Try again. ");
        break;

      }

      else {

        return 0;
      } // closes If loop
    } // closes While loop 
  } // closes Try block code
  
  catch(IllegalArgumentException LawCosExA) { 
    //the variable name is an acronym for "Law Of Cosines Exception" A 
    System.out.println("Sorry! One of your values is out of the range "
        + "for this equation. Please try again.");
    //search.nextLine();
  } // closes Catch code block
  
  catch(ArithmeticException LawCosExB) { //"Law of Cosines Exception" B
    
  }
  
  finally {
    
  } */

  
} //closes IdeasStorage class
